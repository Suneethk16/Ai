services:
  - type: web
    name: ai-study-companion
    env: node
    buildCommand: npm install && npm run build && ls -la dist/
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production